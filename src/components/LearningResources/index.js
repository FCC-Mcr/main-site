import React from "react"
import Carousel from "../Carousel"
import Card from "../Card"

const index = props => {
  return (
    <Carousel>
      <Card height={3}>
        <h1>blah blah</h1>
      </Card>
    </Carousel>
  )
}

export default index
